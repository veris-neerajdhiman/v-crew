apiVersion: v1
kind: ReplicationController
metadata:
  name: org-service
  labels:
    name: org-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: org-service
    spec:
      containers:
      - name: auth-service
        image: inforian/org-service:
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        volumeMounts:
        - name: secrets
          mountPath: /etc/secrets
          readOnly: true
      volumes:
      - name: secrets
        secret:

